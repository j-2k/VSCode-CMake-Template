//Includes might be highlighted as errors in intellisense because you might be missing the .vscode file
#include <iostream>

int main() {

    std::cout << "Hello, CMake in VS Code!" << std::endl;

    return 0;
}